import React from "react";
import { CookieConsent } from "react-cookie-consent";

const Cookies = () => {
  return (
    <div>
      <CookieConsent>Test</CookieConsent>
    </div>
  );
};

export default Cookies;
